__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
venv/
*.egg-info/

# Build artifacts
dist/
*.zip

# Local staging dir for the GitHub Pages extension repo (published to gh-pages branch)
.pages/

# Bundled wheels are committed deliberately once pinned; ignore stray downloads
wheels/*.whl.tmp

# Blender backups / crap files
*.blend1
*.blend2
~*

# File & Link Utilities runtime output (never commit)
FileLinkDebugLog.txt
.filelink/
*_filelink_*.blend
*_local.blend

# OS
.DS_Store
Thumbs.db

# Local Claude Code session/memory data — never commit (public repo)
.claude/
